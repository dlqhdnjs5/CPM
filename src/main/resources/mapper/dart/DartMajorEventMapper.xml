<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bowon.cpm.dart.mapper.DartMajorEventMapper">

    <insert id="insertIgnore" parameterType="com.bowon.cpm.dart.domain.DartMajorEvent"
            useGeneratedKeys="true" keyProperty="id">
        INSERT IGNORE INTO dart_major_event (
            corp_code, stock_code, receipt_no, event_type,
            event_title, event_date, importance_score, summary, raw_json
        ) VALUES (
            #{corpCode}, #{stockCode}, #{receiptNo}, #{eventType},
            #{eventTitle}, #{eventDate}, #{importanceScore}, #{summary}, #{rawJson}
        )
    </insert>

    <!-- 종목코드 기준 최근 N건 (최신순) -->
    <select id="findByStockCode"
            resultType="com.bowon.cpm.dart.domain.DartMajorEvent">
        SELECT id, corp_code, stock_code, receipt_no, event_type,
               event_title, event_date, importance_score, summary, created_at
        FROM dart_major_event
        WHERE stock_code = #{stockCode}
        ORDER BY event_date DESC
        LIMIT #{limit}
    </select>

    <!-- summary 업데이트 -->
    <update id="updateSummary">
        UPDATE dart_major_event
        SET summary = #{summary}
        WHERE id = #{id}
    </update>

    <!-- 종목 + event_date since 이후 (AI 판단 프롬프트용) -->
    <select id="findByStockCodeAndDateAfter"
            resultType="com.bowon.cpm.dart.domain.DartMajorEvent">
        SELECT id, corp_code, stock_code, receipt_no, event_type,
               event_title, event_date, importance_score, summary, created_at
        FROM dart_major_event
        WHERE stock_code = #{stockCode}
          AND event_date IS NOT NULL
          AND event_date >= #{since}
        ORDER BY event_date DESC
        LIMIT #{limit}
    </select>

</mapper>

