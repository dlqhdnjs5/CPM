<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bowon.cpm.ai.mapper.AiDecisionMapper">
    <insert id="insert" parameterType="com.bowon.cpm.ai.domain.AiDecision"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_decision (
            stock_code, stock_name, decision, confidence,
            current_price, target_price, stop_loss_price,
            expected_return_rate, expected_loss_rate, risk_reward_ratio,
            recommended_portfolio_weight, expected_holding_days,
            risk_level, reason, raw_response_id, decision_status, created_at
        ) VALUES (
            #{stockCode}, #{stockName}, #{decision}, #{confidence},
            #{currentPrice}, #{targetPrice}, #{stopLossPrice},
            #{expectedReturnRate}, #{expectedLossRate}, #{riskRewardRatio},
            #{recommendedPortfolioWeight}, #{expectedHoldingDays},
            #{riskLevel}, #{reason}, #{rawResponseId}, #{decisionStatus}, NOW()
        )
    </insert>

    <select id="findById" parameterType="long"
            resultType="com.bowon.cpm.ai.domain.AiDecision">
        SELECT id, stock_code, stock_name, decision, confidence,
               current_price, target_price, stop_loss_price,
               expected_return_rate, expected_loss_rate, risk_reward_ratio,
               recommended_portfolio_weight, expected_holding_days,
               risk_level, reason, raw_response_id, decision_status, created_at
        FROM ai_decision WHERE id = #{id}
    </select>

    <select id="findByStockCode"
            resultType="com.bowon.cpm.ai.domain.AiDecision">
        SELECT id, stock_code, stock_name, decision, confidence,
               current_price, target_price, stop_loss_price,
               expected_return_rate, expected_loss_rate, risk_reward_ratio,
               recommended_portfolio_weight, expected_holding_days,
               risk_level, reason, raw_response_id, decision_status, created_at
        FROM ai_decision
        WHERE stock_code = #{stockCode}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <update id="updateDecisionStatus">
        UPDATE ai_decision
        SET decision_status = #{decisionStatus}
        WHERE id = #{id}
    </update>

    <select id="findByDecisionStatus" resultType="com.bowon.cpm.ai.domain.AiDecision">
        SELECT id, stock_code, stock_name, decision, confidence,
               current_price, target_price, stop_loss_price,
               expected_return_rate, expected_loss_rate, risk_reward_ratio,
               recommended_portfolio_weight, expected_holding_days,
               risk_level, reason, raw_response_id, decision_status, created_at
        FROM ai_decision
        WHERE decision_status = #{decisionStatus}
          AND created_at >= CURDATE()
        ORDER BY created_at DESC
    </select>

    <!--
        주문 실행 스케줄러 전용.
        - decision_status = #{decisionStatus} (CREATED) + decision = 'BUY'
        - created_at >= #{since}: 직전 사이클 이내의 "신선한" 판단만 사용
        - 같은 종목 중복 시 가장 최신 1건만 반환 (자체 조인으로 stock_code별 MAX(created_at) 추출)
        - 인덱스 활용을 위해 created_at에 함수 래핑 사용 안 함
    -->
    <select id="findPendingBuyDecisions" resultType="com.bowon.cpm.ai.domain.AiDecision">
        SELECT a.id, a.stock_code, a.stock_name, a.decision, a.confidence,
               a.current_price, a.target_price, a.stop_loss_price,
               a.expected_return_rate, a.expected_loss_rate, a.risk_reward_ratio,
               a.recommended_portfolio_weight, a.expected_holding_days,
               a.risk_level, a.reason, a.raw_response_id, a.decision_status, a.created_at
        FROM ai_decision a
        INNER JOIN (
            SELECT stock_code, MAX(created_at) AS max_created
            FROM ai_decision
            WHERE decision_status = #{decisionStatus}
              AND decision = 'BUY'
              AND created_at >= #{since}
            GROUP BY stock_code
        ) latest
          ON latest.stock_code = a.stock_code
         AND latest.max_created = a.created_at
        WHERE a.decision_status = #{decisionStatus}
          AND a.decision = 'BUY'
          AND a.created_at >= #{since}
        ORDER BY a.created_at DESC
    </select>

    <update id="expireStaleDecisions">
        UPDATE ai_decision
        SET decision_status = 'EXPIRED'
        WHERE decision_status = 'CREATED'
          AND created_at &lt; #{before}
    </update>

    <select id="findTodayDecisions" resultType="com.bowon.cpm.ai.domain.AiDecision">
        SELECT id, stock_code, stock_name, decision, confidence,
               current_price, target_price, stop_loss_price,
               expected_return_rate, expected_loss_rate, risk_reward_ratio,
               recommended_portfolio_weight, expected_holding_days,
               risk_level, reason, raw_response_id, decision_status, created_at
        FROM ai_decision
        WHERE DATE(created_at) = CURDATE()
          AND decision IN ('BUY', 'SELL')
        ORDER BY created_at DESC
    </select>

    <!--
        보유 기간 만기 도래 + HOLDING_END 미평가 판단 조회.
        - decision IN ('BUY','SELL')
        - expected_holding_days NOT NULL
        - DATE(created_at) + INTERVAL expected_holding_days DAY <= #{today}
        - 같은 ai_decision_id로 HOLDING_END 피드백이 아직 없음
    -->
    <select id="findHoldingDayMaturedDecisions"
            resultType="com.bowon.cpm.ai.domain.AiDecision">
        SELECT a.id, a.stock_code, a.stock_name, a.decision, a.confidence,
               a.current_price, a.target_price, a.stop_loss_price,
               a.expected_return_rate, a.expected_loss_rate, a.risk_reward_ratio,
               a.recommended_portfolio_weight, a.expected_holding_days,
               a.risk_level, a.reason, a.raw_response_id, a.decision_status, a.created_at
        FROM ai_decision a
        LEFT JOIN ai_feedback f
          ON f.ai_decision_id = a.id
         AND f.evaluation_type = 'HOLDING_END'
        WHERE a.decision IN ('BUY','SELL')
          AND a.expected_holding_days IS NOT NULL
          AND DATE_ADD(DATE(a.created_at), INTERVAL a.expected_holding_days DAY) &lt;= #{today}
          AND f.id IS NULL
        ORDER BY a.created_at ASC
    </select>

    <!-- 기간 내 생성된 BUY/SELL 판단 (WEEKLY/MONTHLY 통계용) -->
    <select id="findDecisionsBetween"
            resultType="com.bowon.cpm.ai.domain.AiDecision">
        SELECT id, stock_code, stock_name, decision, confidence,
               current_price, target_price, stop_loss_price,
               expected_return_rate, expected_loss_rate, risk_reward_ratio,
               recommended_portfolio_weight, expected_holding_days,
               risk_level, reason, raw_response_id, decision_status, created_at
        FROM ai_decision
        WHERE created_at &gt;= #{from}
          AND created_at &lt; #{to}
        ORDER BY created_at ASC
    </select>

</mapper>

