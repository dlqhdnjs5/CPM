<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bowon.cpm.news.mapper.NewsAiSummaryMapper">

    <insert id="insertIgnore" parameterType="com.bowon.cpm.news.domain.NewsAiSummary"
            useGeneratedKeys="true" keyProperty="id">
        INSERT IGNORE INTO news_ai_summary (
            news_id, stock_code, summary, key_points,
            model_name, prompt_tokens, completion_tokens, created_at
        ) VALUES (
            #{newsId}, #{stockCode}, #{summary}, #{keyPoints},
            #{modelName}, #{promptTokens}, #{completionTokens}, NOW()
        )
    </insert>

</mapper>
