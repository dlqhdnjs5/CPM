<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bowon.cpm.risk.mapper.RiskPolicyConfigMapper">

    <select id="findByPolicyCode" parameterType="string"
            resultType="com.bowon.cpm.risk.domain.RiskPolicyConfig">
        SELECT id, policy_code, policy_name,
               min_confidence, max_position_weight, max_order_amount,
               min_risk_reward_ratio, max_expected_loss_rate, block_overheat_rate,
               is_active
        FROM risk_policy_config
        WHERE policy_code = #{policyCode}
          AND is_active = 1
        LIMIT 1
    </select>

    <select id="findAllActive"
            resultType="com.bowon.cpm.risk.domain.RiskPolicyConfig">
        SELECT id, policy_code, policy_name,
               min_confidence, max_position_weight, max_order_amount,
               min_risk_reward_ratio, max_expected_loss_rate, block_overheat_rate,
               is_active
        FROM risk_policy_config
        WHERE is_active = 1
        ORDER BY policy_code
    </select>

    <update id="updateSelective" parameterType="com.bowon.cpm.risk.domain.RiskPolicyConfig">
        UPDATE risk_policy_config
        <set>
            <if test="policyName != null">policy_name = #{policyName},</if>
            <if test="minConfidence != null">min_confidence = #{minConfidence},</if>
            <if test="maxPositionWeight != null">max_position_weight = #{maxPositionWeight},</if>
            <if test="maxOrderAmount != null">max_order_amount = #{maxOrderAmount},</if>
            <if test="minRiskRewardRatio != null">min_risk_reward_ratio = #{minRiskRewardRatio},</if>
            <if test="maxExpectedLossRate != null">max_expected_loss_rate = #{maxExpectedLossRate},</if>
            <if test="blockOverheatRate != null">block_overheat_rate = #{blockOverheatRate},</if>
            <if test="isActive != null">is_active = #{isActive},</if>
            updated_at = NOW()
        </set>
        WHERE policy_code = #{policyCode}
    </update>

</mapper>

