# 전체 프로세스 간략 정리

## 1. 모니터링 종목 추가

관심 종목을 AI 판단 대상으로 준비한다.

```powershell
curl.exe -X POST "http://localhost:8080/api/stocks/{stockCode}/ai-data/bootstrap?from=2026-03-01&to=2026-06-05&newsDisplay=30&newsAnalyzeLimit=0"
```

수집되는 데이터:

```text
stock_master
dart_disclosure
dart_major_event
dart_financial_statement
stock_news
stock_realtime_quote
stock_price_daily
stock_indicator_daily
```

## 2. 뉴스 AI 분석

뉴스 원문 수집 후 AI 요약/감성 분석을 따로 실행한다.

```powershell
curl.exe -X POST "http://localhost:8080/api/news/analyze?limit=20"
```

저장 테이블:

```text
news_ai_summary
news_sentiment
```

## 3. AI 판단 생성

`AiDecisionScheduler`가 active 종목을 대상으로 AI 판단을 생성한다.

```text
stock_master active 종목
→ 시세/일봉/지표/뉴스/공시/재무/피드백 조회
→ OpenAI 호출
→ ai_decision 저장
```

초기 상태:

```text
ai_decision.decision_status = CREATED
```

## 4. 리스크 검증

`OrderExecutionScheduler`가 최근 `CREATED` 상태의 `BUY/SELL` 판단을 찾는다.

```text
ai_decision
→ risk_check_result
```

`HOLD`는 주문 대상이 아니다.

## 5. 주문 처리

리스크 검증을 통과하면 주문 요청을 만든다.

```text
risk_check_result passed=true
→ order_request READY
→ OrderExecutor 실행
```

PAPER 모드:

```text
order_request
→ order_execution
→ paper_portfolio_position
→ paper_account_balance
```

REAL 모드:

```text
KIS 주문 API
→ order_execution
→ portfolio_position
→ account_balance
```

## 6. 보유 상태 확인

PAPER:

```powershell
curl.exe "http://localhost:8080/api/account/paper/balance"
curl.exe "http://localhost:8080/api/account/paper/positions"
```

REAL:

```powershell
curl.exe "http://localhost:8080/api/account/balance"
curl.exe "http://localhost:8080/api/account/positions"
```

## 7. 주문/체결 확인

```powershell
curl.exe "http://localhost:8080/api/orders/requests?mode=PAPER&limit=50"
curl.exe "http://localhost:8080/api/orders/executions?stockCode=005930&mode=PAPER&limit=50"
```

## 8. 피드백

장 마감 후 AI 판단 결과를 평가한다.

```text
ai_decision
→ ai_feedback
→ WEEKLY / MONTHLY 요약
→ 다음 AI 판단 프롬프트 참고자료
```

현재 AI 프롬프트에 직접 들어가는 피드백 타입:

```text
WEEKLY
MONTHLY
HOLDING_END
```

`DAILY` 피드백은 저장되지만 현재 프롬프트에서는 제외된다.

## 9. 핵심 테이블 흐름

```text
데이터 준비
→ stock_master / stock_news / dart_* / stock_price_daily / stock_indicator_daily

AI 판단
→ ai_prompt_log / ai_decision_raw_response / ai_decision / ai_decision_factor

리스크
→ risk_check_result

주문
→ order_request / order_status_history / order_execution

보유 상태
→ paper_portfolio_position 또는 portfolio_position

피드백
→ ai_feedback / ai_periodic_summary
```

## 10. 한 줄 요약

```text
종목 데이터 수집
→ AI 판단
→ 리스크 검증
→ PAPER/REAL 주문
→ 체결/포트폴리오 반영
→ 피드백 저장
→ 다음 AI 판단에 반영
```
